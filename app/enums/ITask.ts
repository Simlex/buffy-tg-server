export enum Task {
    TELEGRAM = "telegram",
    TWITTER = "twitter",
    WALLET_CONNECT = "walletConnect",
    TON_TRANSACTION = "tonTransaction",
}

export enum TaskType {
    Social,
    Referral,
    Others
}